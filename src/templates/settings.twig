{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
  label: "API Key"|t('convergine-brevo'),
  instructions: "The Brevo API key."|t('convergine-brevo'),
  id: 'api_key',
  name: 'api_key',
  value: adapter.api_key,
  suggestEnvVars: true,
  errors: adapter.getErrors('api_key')
}) }}
